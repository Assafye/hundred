global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts/feed_alert_rules.yml

scrape_configs:
  - job_name: feed-api
    metrics_path: /metrics
    static_configs:
      - targets: ['feed-api:8000']
